def handle(req):
    """
    Handle function receives the request body as a string
    Args:
        req (str): request body
    Returns:
        str: response body
    """
    return "Hello from OpenFaaS!"
